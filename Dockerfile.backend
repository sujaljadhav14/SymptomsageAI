FROM node:20-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install --production

# Copy the rest of the code
COPY . .

# Only the server is needed here, but since the repo is shared, we copy everything 
# and just run the server script.

EXPOSE 3001

CMD ["npm", "run", "server"]
